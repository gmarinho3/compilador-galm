let multi = 1 + 1 * 2;

assertEquals(multi, 3);

let div = 1.0 + 2.0 / 2.0 - 1.0;

assertEquals(div, 1.0);

let exp = (1.0 + 1.0) * 2.0;
let exponential = 1.0 * 2.0 ** 2.0 * 4.0;

let multiDiv = 2.0 * 2.0 / 4.0;
let divMulti = -2.0 / 2.0 * 4.0;

let mod = 3 % 2;
let modMultiDiv = 2 % 2 / 2 ;

assertEquals(mod, 1);
assertEquals(modMultiDiv, 0);
assertEquals(exponential, 16);